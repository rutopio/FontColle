# Python deps for the daily harvest. daily_update -> to_dataset.apply_specimens
# -> langcov needs the full Google Fonts language stack, not just fonttools.
# Pinned to the set verified locally (CLAUDE.md: pin exact versions).
fonttools==4.60.1
gftools==0.9.996
gflanguages==0.7.7
langcodes==3.5.1
