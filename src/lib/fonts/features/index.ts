// Barrel for the OpenType feature metadata, split into names (detail tester)
// and groups (filter panel). Import from "@/lib/fonts/features" as before.

export * from "./groups";
export * from "./names";
