# `src/data` — where each file and field comes from

This directory is the dataset the site is built from. Everything here is
**generated by the harvester** (`scripts/harvester/`), not hand-edited. This
README documents the provenance of each file and, for `fonts.json`, of each
field — so a wrong value can be traced to the upstream source that produced it.

See the repo README's [Data pipeline](../../README.md#data-pipeline) for the
build/publish/deploy flow. This file is only about origins.

## Files

| File | In git? | Produced by | Upstream source |
| --- | --- | --- | --- |
| `fonts.json` | no (R2) | the full harvest + backfills | google/fonts repo + Google Fonts APIs + the font binaries |
| `data-manifest.json` | no (R2) | `scripts/publish-assets.mjs` | pointer to the R2 snapshot the build pulls; lives at `manifest/latest.json` in R2 |
| `fonts.sample.json` | yes | committed 24-family excerpt of `fonts.json` | fallback so a fork without R2 access can still build |
| `scripts.json` | yes | `to_dataset.py` (`write_label_maps`) | derived label map: script code → human name, for the scripts the catalog uses. Names from `gflanguages` |
| `languages.json` | yes | `to_dataset.py` (`write_label_maps`) | derived label map: language code → human name, for the languages the catalog uses. Names from `gflanguages` |
| `axes.json` | yes | `scripts/gen-axes-data.mjs` (one-off) | [googlefonts/axisregistry](https://github.com/googlefonts/axisregistry) — variable-axis registry (name, min/default/max, fallbacks) |
| `vendors.json` | yes | `scripts/gen-vendors-data.mjs` (one-off) | [Microsoft OpenType vendor ID registry](https://learn.microsoft.com/en-us/typography/vendors/) — the OS/2 `achVendID` → foundry name map |

`scripts.json` / `languages.json` are regenerated on every daily harvest but
change only when a family introduces a brand-new writing system or language, so
they rarely move (and CI does not auto-commit them). `axes.json` / `vendors.json`
are one-off reference tables refreshed manually by re-running their generators.

## `fonts.json` fields, by source

Each family record is assembled from four kinds of source. A field is null when
its source doesn't cover that family (e.g. brand fonts served only via the API
have no repo, so repo-only fields stay null).

### 1. Parsed from the font binary (the primary TTF) — `harvest.py` → `parse_ttf`

Read straight out of the `OS/2`, `hhea`, `post`, `head`, `name`, `fvar`,
`GSUB`/`GPOS`, `STAT` and color tables of the downloaded TTF:

- **Metrics**: `unitsPerEm`, `xHeight`, `capHeight`, `avgCharWidth`,
  `hheaAscender`/`hheaDescender`/`hheaLineGap`,
  `typoAscender`/`typoDescender`/`typoLineGap`, `winAscent`/`winDescent`,
  `italicAngle`, `useTypoMetrics`.
- **OS/2 classification**: `weightClass` (usWeightClass), `widthClass`
  (usWidthClass), `fsType` (embedding-permission bits), `panose`.
- **`vendorId`** — the OS/2 `achVendID`; resolved to a foundry name via
  `vendors.json` (Microsoft's registry).
- **Outline / feature facts**: `glyphCount`, `charCount`, `isMonospace`,
  `hasHinting`, `hasStat`, `colorTables`, `isVariable`, `fileSize`.
- **Timestamps from the `head` table**: `createdMs`, `modifiedMs` (the font's
  own created/modified dates, OpenType-epoch → unix ms).
- **Variable-font data**: `axes` (the fvar axes; tag/min/default/max — axis
  names/ranges cross-referenced with `axes.json` from the Google axis registry),
  `instances` (named instances), `weights` (derived from the wght axis or static
  files).
- **OpenType features**: `features` — the union of `GSUB` + `GPOS` feature tags.
- **Version**: `version` (head.fontRevision), `versionString` (name ID 5).

### 2. From the family's `METADATA.pb` in the google/fonts repo — `harvest.py`, `backfill_source.py`

- `designer`, `subsets`, `dateAdded`, `apiCategory` (Google's coarse category),
  `licenseDir` (ofl / apache / ufl), `primaryTtf` (the primary file name).
- `repositoryUrl` — the `source { repository_url }` block
  (`backfill_source.py`).

### 3. Google Fonts web APIs (not the repo)

- **Developer API** (`webfonts/v1`, via `fetch_published.py`) → `isPublished`,
  `isOpenSource`, `lastModifiedApi`, `isNoto`, `isBrandFont`, and the ranking
  signals `popularityRank` / `trendingRank`.
- **Metadata endpoint** (`fonts.google.com/metadata/fonts`):
  - `displayName` (`backfill_display_names.py`).
  - `about` + `designerProfiles` (`backfill_about.py`).
- **google/fonts tag data** (raw CSV on `main`):
  - `tags` — `tags/all/families.csv`, per-family classification scores 0–100
    (`backfill_tags.py`). `category` / `facets` are **derived** from these tag
    scores (`backfill_form_category.py`), which is finer than `apiCategory`.
  - `contrast` — `tags/all/quant.csv` stroke-width min/max (`backfill_contrast.py`).
- **google/fonts git history** (GitHub commits API):
  - `versionHistory`, `firstCommitDate` (`backfill_version_history.py`).
- **license text**: per-family `OFL.txt` on `main` → `licenseHeader`
  (`backfill_license.py`).

### 4. Derived locally by the harvester

- **Language / script coverage** (`langcov.py`, from `gflanguages` +
  `gftools`): `languages`, `scripts`, `primaryScript`, `cjkCoverage`,
  `specimen` (a preview string in the family's own script).
- **Computed flags / ids**: `id` (slug), `name`, `category` (final class),
  `facets`.
- **Glyph coverage** is written separately to `public/glyphs/<id>.json`
  (`backfill_glyph_coverage.py`, reading `ttf_cache/`), not stored inside
  `fonts.json`. It backs the Glyphs panel, and `scripts/gen-glyph-index.mjs`
  folds all of it into one deduplicated `public/glyph-index.json` at build time
  for the list page's preview-coverage filter.

## Quick lookup

| Field(s) | Source |
| --- | --- |
| `axes`, `instances`, `weights`, `features`, all metrics, `panose`, `vendorId`, `fsType`, `weightClass`, `widthClass`, `glyphCount`, `charCount`, `fileSize`, `createdMs`, `modifiedMs`, `version` | the font binary (TTF) |
| `vendorId` name resolution | `vendors.json` ← Microsoft vendor registry |
| axis names/ranges | `axes.json` ← googlefonts/axisregistry |
| `designer`, `subsets`, `apiCategory`, `licenseDir`, `dateAdded`, `repositoryUrl` | `METADATA.pb` (google/fonts repo) |
| `popularityRank`, `trendingRank`, `isPublished`, `isOpenSource`, `isNoto`, `isBrandFont`, `lastModifiedApi` | Developer API |
| `displayName`, `about`, `designerProfiles` | metadata endpoint |
| `tags`, `category`, `facets`, `contrast` | google/fonts tag CSVs |
| `versionHistory`, `firstCommitDate`, `licenseHeader` | google/fonts git / license files |
| `languages`, `scripts`, `primaryScript`, `cjkCoverage`, `specimen` | `langcov.py` ← gflanguages |
