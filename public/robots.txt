# https://www.robotstxt.org/robotstxt.html
User-agent: *
Disallow:

# Filter params on `/` are transient views of one catalog, not distinct pages
# (the route's canonical already points every filtered view at the bare `/`).
# They are also an effectively infinite URL space: a crawler walking
# ?designer= / ?avgwidth= / ?fav= combinations generates unbounded requests for
# pages whose SSR body is only a loading skeleton. Every detail page and the
# bare `/` stay crawlable; only the permutations are off-limits.
Disallow: /?

# Content Signals (contentsignals.org, draft-romm-aipref-contentsignals).
# This is an open-source font catalog: search and AI answers may reference it,
# but the pages are not offered as AI training material.
Content-Signal: search=yes, ai-input=yes, ai-train=no

# LLMs / AI search tools: machine-readable catalog guide at
# https://fontcolle.com/llms.txt (field tables, tag vocabulary, URL params).

Sitemap: https://fontcolle.com/sitemap.xml
